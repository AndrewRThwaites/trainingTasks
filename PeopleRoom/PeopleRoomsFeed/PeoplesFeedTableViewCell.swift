//
//  PeoplesFeedTableViewCell.swift
//  PeopleRoom
//
//  Created by Andrew Thwaites on 21/09/2022.
//

import UIKit

class PeopleFeedTableViewCell: UITableViewCell {
/*
    let newsImageView: UIImageView = {
        let imgView = UIImageView()
        imgView.translatesAutoresizingMaskIntoConstraints = false
        imgView.image = UIImage(named: "default")
        return imgView
    }()
    private let newsView: UIView = {
        let view = UIView()
        view.translatesAutoresizingMaskIntoConstraints = false
        return view
    }()
    private let newsTitleLabel: UILabel = {
        let lbl = UILabel()
        lbl.numberOfLines = 0
        lbl.font = UIFont.systemFont(ofSize: 19, weight: UIFont.Weight.bold)
        lbl.translatesAutoresizingMaskIntoConstraints = false
        return lbl
    }()
    private let commentsCountLabel: UILabel = {
        let lbl = UILabel()
        lbl.translatesAutoresizingMaskIntoConstraints = false
        return lbl
    }()
    private let scoreLabel: UILabel = {
        let lbl = UILabel()
        lbl.translatesAutoresizingMaskIntoConstraints = false
        return lbl
    }()
    */
    override func awakeFromNib() {
   
    }

    override func setSelected(_ selected: Bool, animated: Bool) {
        super.setSelected(selected, animated: animated)

        // Configure the view for the selected state
    }

}
